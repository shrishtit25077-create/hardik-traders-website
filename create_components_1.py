"import os\n\nos.makedirs('src/components/home', exist_ok=True)\n\n# 1. HomeNavbar.jsx\nhome_navbar = \"\"\"import React, { useState, useEffect } from 'react';\nimport { Link } from 'react-router-dom';\nimport { motion } from 'framer-motion';\n\nconst HomeNavbar = () => {\n  const [scrolled, setScrolled] = useState(false);\n  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);\n\n  useEffect(() => {\n    const handleScroll = () => setScrolled(window.scrollY > 50);\n    window.addEventListener('scroll', handleScroll);\n    return () => window.removeEventListener('scroll', handleScroll);\n  }, []);\n\n  return (\n    <motion.header \n      initial={{ y: -100 }}\n      animate={{ y: 0 }}\n      className={`fixed top-0 w-full z-50 transition-all duration-300 ${scrolled ? 'bg-background/80 backdrop-blur-md shadow-[0_4px_30px_rgba(225,6,0,0.1)] border-b border-primary/20' : 'bg-transparent border-b border-transparent'}`}\n    >\n      <div className=\"max-w-[1400px] mx-auto px-margin-mobile md:px-margin-desktop h-[80px] flex items-center justify-between\">\n        <Link to=\"/\" className=\"flex items-center gap-2 group\">\n          <span className=\"material-symbols-outlined text-primary text-[36px] group-hover:rotate-180 transition-transform duration-700\">settings_input_component</span>\n          <span className=\"font-headline-md text-on-surface uppercase tracking-tighter\">Hardik <span className=\"text-primary\">Traders</span></span>\n        </Link>\n        \n        <nav className=\"hidden md:flex items-center gap-8\">\n          {['Home', 'Products', 'Brands', 'Industries', 'Resources', 'Contact'].map((item) => (\n            <Link key={item} to={item === 'Home' ? '/' : `/${item.toLowerCase()}`} className=\"text-on-surface-variant hover:text-white transition-colors font-label-bold uppercase text-[13px] tracking-wider relative group\">\n              {item}\n              <span className=\"absolute -bottom-1 left-0 w-0 h-[2px] bg-primary group-hover:w-full transition-all duration-300\"></span>\n    
<truncated 14695 bytes>